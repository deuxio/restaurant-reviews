<% content_for :title do 'La Cuillère - The One & Only' end %>

<div class="container">
  <div class="row">
    <div class="col-xs-8 col-xs-offset-1">
      <div class="page-header">
        <h1>Restaurants list</h1>
      </div>
      <% @restaurants.each do |restaurant| %>
        <div class="restaurants">
          <h2 id="restoname"><%= link_to restaurant.name, restaurant_path(restaurant) %></h2>
          <span class="restaurantlistdetails"><i class="fa fa-comments"></i> <%= restaurant.reviews.count %></span>
          <span class="restaurantlistdetails"><i class="fa fa-fire"></i>  <%= restaurant.category %></span>
        </div>
      <% end %>
    </div>
  </div>
</div>